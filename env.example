# Generate a secret key with: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=replace-with-a-random-secret-key

DB_HOST=localhost
DB_PORT=5433
DB_NAME=TINQAD_Database
DB_USER=postgres
DB_PASSWORD=postgres